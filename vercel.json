{
  "builds": [{ "src": "index.js", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "/index.js" }]
  
  "functions": {
    "api/**/*.js": {
      "maxDuration": 60 // Set this to a higher value as needed, within Vercel's limits
    }
  
}
}
